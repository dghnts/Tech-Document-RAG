# **基本設計書 (Basic Design)**

## **1\. システム構成**

本アプリは、外部APIとインメモリ処理を組み合わせたサーバーレス構造を採用する。

### **1.1 技術スタック**

* **Frontend**: Streamlit  
* **LLM**: Google Gemini 1.5 Flash (API)  
* **RAG Framework**: LangChain  
* **Vector DB**: FAISS (In-memory mode)  
* **Embedding**: intfloat/multilingual-e5-small (Hugging Face / Sentence Transformers)

## **2\. 処理設計 (Phase 1: 単一ページ読み込み)**

### **2.1 データ抽出 (Extract)**

* WebBaseLoader を用いて指定URLへリクエストを送信。  
* BeautifulSoupを利用し、不要なタグ（script, style, nav, footer等）を排除した本文（main content）のみを取得。

### **2.2 テキスト分割 (Split)**

* RecursiveCharacterTextSplitter を採用。  
* **Chunk Size**: 1,000文字  
* **Chunk Overlap**: 100文字

### **2.3 ベクトル化・保存 (Vectorize & Store)**

* 分割されたテキストを multilingual-e5-small で数値化。  
* FAISSインデックスをメモリ上に構築。ストレージへの永続化は行わない。

### **2.4 検索・生成 (Search & Generate)**

1. ユーザーの質問をベクトル化。  
2. 類似度の高い上位3件程度のチャンク（文脈）を抽出。  
3. 抽出した情報とユーザーの質問を結合したプロンプトを生成。  
4. Gemini APIへ送信し、回答を取得。

## **3\. 画面設計**

### **3.1 コンポーネント構成**

* **サイドバー (Sidebar)**:  
  * APIキー入力欄（st.text\_input / type="password"）  
  * 設定状況のステータス表示  
* **メインエリア (Main Area)**:  
  * アプリタイトル  
  * ドキュメントURL入力ボックス  
  * 「解析開始」ボタン（処理中は st.spinner を表示）  
  * チャットインターフェース（質問入力ボックスと履歴の表示）

### **3.2 制御ロジック**

* **入力バリデーション**: APIキーまたはURLが未入力の場合、解析ボタン押下時に警告を表示する。  
* **セッション管理**: st.session\_state を活用し、チャット履歴やベクトルDBのインスタンスを保持する。